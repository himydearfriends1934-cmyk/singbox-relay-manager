{
  "name": "singbox-relay-manager",
  "version": "0.1.0",
  "description": "Small HK relay + US exit config manager for sing-box and OpenClash.",
  "type": "module",
  "bin": {
    "relaykit": "./src/cli.js"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "start": "node src/panel.js",
    "cli": "node src/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
